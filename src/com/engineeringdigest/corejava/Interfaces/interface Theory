1) Interface is blueprint for class.
2) It contains abstract methods, static methods, static constraints and default methods.
3) Use cases -> Mutiple Inheritance & Abstraction
4) Eska object nahi ban sakta, and abstract classes ka bhi nahi banta.
5) It means ki constructor bhi nahi banega.
6) Static constants means ki vo public to honge hee, static bhi, and final bhi by default.
7) Interface can have static and default methods.
8) Esme jo hum static methods banate hai vo sirf interface se hee access kar sakte hai not by extending class objects
9) default methods jab hum banate hai to vo directly instance se call kar sakte hai and vo interface me koi error nahi dega.
10) Agar hum normal abstract methods banate hai to use fir override karana padega in implementing class, this will create complexity,
11) that's why we use static and default methods.
12) Interface methods by default public and abstract hote hai.